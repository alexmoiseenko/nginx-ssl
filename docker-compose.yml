version: '3'
services:
    nginx:
        image: nginx
        ports :
            - "8000:443/tcp"
        volumes: 
            - ./nginx/certs:/etc/nginx/certs:ro
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
            - ./web:/var/www:ro
        
    
